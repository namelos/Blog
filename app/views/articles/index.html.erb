<section class="content">
  <h1 class="content-title">Namelos</h1>

  <ul>
    <% @articles.each do |article| %>
        <li><%= link_to article.title, article_path(article) %></li>
    <% end %>
  </ul>

  <% if logged_in? %>
    <td><%= link_to 'New', new_article_path %></td>
  <% end %>
</section>

