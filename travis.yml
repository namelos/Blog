services:
  - postgresql
language: ruby
rvm:
  - "2.3.0"
before_script:
  - psql -c 'create database travis_ci_test;' -U postgres
test:
  adapter: postgresql
  database: travis_ci_test
script: rails t
